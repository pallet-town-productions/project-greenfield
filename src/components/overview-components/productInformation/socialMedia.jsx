import React from 'react';
import PT from 'prop-types';
import { connect } from 'react-redux';

const SocialMediaButtons = function ({}) {
  return (
    
  );
}

export default connect(null, null)(SocialMediaButtons);